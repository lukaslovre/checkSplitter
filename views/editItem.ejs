<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/newItem.css" />
    <script src="/js/editItem.js" defer></script>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500&display=swap"
      rel="stylesheet"
    />
    <link
      href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500&display=swap"
      rel="stylesheet"
    />
    <title>Check Splitter</title>
  </head>
  <body>
    <a href="/"> <h1>Lista kupovina</h1></a>
    <main>
      <% if(locals.item){ %>
      <form
        action="/editItem"
        method="post"
        autocomplete="off"
        spellcheck="false"
        enctype="multipart/form-data"
      >
        <input
          type="text"
          name="id"
          id="id"
          value="<%= item._id %>"
          style="display: none"
        />
        <label for="title">Naslov</label>
        <input
          type="text"
          name="title"
          id="title"
          value="<%= item.title %>"
          required
        />
        <label for="date">Datum</label>
        <input
          type="text"
          name="date"
          id="date"
          value="<%= item.date %>"
          required
        />
        <label for="notes">Bilješke</label>
        <input type="text" name="notes" id="notes" value="<%= item.notes %>" />
        <div class="memberInputDiv">
          <p>Tko je sudjelovao</p>
          <% for(let i=0; i<item.members.length;i++){%>
          <input type="checkbox" name="members" id="member<%= i+1%>" value="<%= item.members[i]%>" checked disabled/>
          <label for="member<%= i+1%>"><%= item.members[i]%></label>
          <br />
          <% } %>
          <input
            type="checkbox"
            name="members"
            id="member<%= item.members.length+1%>"
            value=""
            class="customMember"
          />
          <input type="text" class="checkboxTextInput customMember" />
        </div>
        <label for="img">Slika računa</label>
        <input type="file" id="img" name="img" accept="image/*" />
        <input type="submit" value="Edit" />
      </form>
      <% } %>
    </main>
  </body>
</html>
